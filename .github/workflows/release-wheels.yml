# .github/workflows/release-wheels.yml
name: release-wheels

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wheels:
    name: wheels (${{ matrix.os }} cp${{ matrix.python-version-no-dot }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"
          - "3.13"
        os:
          - ubuntu-latest
          - windows-latest
          - macos-15-intel  # x86_64
          - macos-15        # arm64
        include:
          - python-version: "3.11"
            python-version-no-dot: "311"
          - python-version: "3.12"
            python-version-no-dot: "312"
          - python-version: "3.13"
            python-version-no-dot: "313"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # Linux: maturin-action builds in a manylinux container; select the requested CPython explicitly.
      - name: Build wheel (linux cp${{ matrix.python-version-no-dot }})
        if: runner.os == 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release --out dist -i python${{ matrix.python-version }}
          manylinux: auto

      # macOS/Windows: use host python from setup-python.
      - name: Build wheel (host cp${{ matrix.python-version-no-dot }})
        if: runner.os != 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release --out dist -i python
          manylinux: auto

      - name: List dist/
        shell: bash
        run: ls -la dist || true

      - name: Smoke test wheel (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          python -m pip install -U pip
          python -m pip install dist/*.whl
          python -c "import smirk; print('import ok')"

      - name: Smoke test wheel (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          python -m pip install -U pip
          $wheel = (Get-ChildItem -Path dist -Filter *.whl | Select-Object -First 1).FullName
          if (-not $wheel) { throw "No wheel found in dist/" }
          python -m pip install $wheel
          python -c "import smirk; print('import ok')"

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}-cp${{ matrix.python-version-no-dot }}
          path: dist/*.whl

  release:
    name: upload wheels to GitHub Release
    needs: wheels
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true

      - name: Attach wheels to release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.whl
